{
  "name": "open-api-nest-experiment",
  "version": "1.0.0",
  "description": "Experiment on open api",
  "repository": "git@github.com:paulsouche/open-api-nest-experiment.git",
  "author": "paulsouche <paul.souche@gmail.com>",
  "license": "MIT",
  "private": true,
  "resolutions": {
    "rimraf": "^3.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream --parallel",
    "lint": "lerna run lint --stream --parallel",
    "test": "lerna run test --stream --parallel"
  },
  "dependencies": {
    "tslib": "^1.10.0"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "tslint": "^5.20.1",
    "typescript": "^3.7.4"
  }
}
